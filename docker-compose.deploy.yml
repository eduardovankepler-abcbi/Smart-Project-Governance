services:
  api:
    ports: []
    environment:
      CORS_ORIGINS: ${CORS_ORIGINS}

  frontend:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-auto}
    ports:
      - "${FRONTEND_PORT:-80}:80"
